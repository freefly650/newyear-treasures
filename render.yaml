# Render Blueprint – deploy as Web Service
# https://docs.render.com/blueprint-spec

services:
  - type: web
    name: newyear-treasures
    runtime: node
    plan: free

    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/auth/config
